# Spire Pilot 2 项目背景与交接说明

更新日期：2026-08-08

## 一、项目目标

本项目希望为《杀戮尖塔 2》构建一个具有较强决策能力的 AI。长期目标不是单纯做一个会调用工具、按固定规则点击游戏的 Agent，而是让模型真正学习游戏状态、动作后果与长期收益之间的关系。

项目目前有两个相互关联的研究方向：

1. **战斗 Agent**：根据手牌、费用、敌人状态、敌人意图、牌堆、遗物和药水等玩家可见信息，选择较优的出牌、目标、药水和结束回合时机。
2. **局外规划 Agent**：负责抓牌、路线、商店、事件、营火、奖励和遗物选择等长线决策。希望通过 World Model 或 Value Model 估计一个选择对后续多个楼层乃至整局胜率的影响。

Spire Pilot 是本项目的重要参考对象。差异化方向是面向《杀戮尖塔 2》，使用真实游戏引擎、可审计的人工对局数据和显式长期价值估计，逐步建立从数据采集、战斗策略到局外规划的完整闭环。

## 二、当前版本边界

现阶段所有工作严格锁定在以下游戏版本：

- 游戏版本：正式版 v0.107.1
- Steam build：23811903
- 游戏主程序集 SHA-256：`A1F9E653F1E28E4076558FEE1E60D218619CB7E057B887C6417F62C62C6D7A52`
- 第一阶段主要角色：铁甲战士
- 数据可包含不同难度，但必须保存角色、难度、Seed、游戏版本和内容来源信息

不同游戏版本的数据不得直接混入同一个版本分区。游戏更新后，需要重新验证实体、动作钩子、状态字段和回放能力，再建立新的版本分区。

项目保留了一份 v0.107.1 的私人离线备份用于兼容性调试。该备份仅限合法拥有游戏的个人研究用途，不应上传到公开仓库，也不应向他人分发。

## 三、已经完成的工作

### 1. HumanRecorder 人工对局记录器

目前已经完成 HumanRecorder 0.5.3-internal。它可以在玩家正常游玩时记录玩家可见状态和语义化决策，用于构建高质量人工数据集。

记录范围已覆盖：

- 地图路线选择
- 战斗出牌、选取目标和结束回合
- 战斗及非战斗药水使用、药水丢弃
- 普通选牌、跳过选牌、多选牌和确认/取消选择
- 事件选项
- 营火选项，包括遗物导致的多次营火选择
- 商店购买、删牌和离开
- 普通奖励、多份奖励和替代奖励
- 宝箱与遗物选择
- 初始卡包、特殊卡牌选择和 Boss 奖励
- 游戏胜利、死亡和主动放弃

记录器保存的不是鼠标坐标，而是“玩家在什么状态下做了什么游戏动作”。这使数据更适合策略学习，也减少了分辨率、界面位置和输入设备造成的噪声。

### 2. 状态信息完整性

战斗观察目前能够记录模型决策所需的主要玩家可见信息：

- 玩家生命、格挡、费用和可见状态
- 手牌、抽牌堆/弃牌堆/消耗堆的玩家可见信息
- 卡牌实例、升级状态、费用变化、附魔和其他卡牌绑定状态
- 敌人稳定 ID、生命、格挡、状态、可见意图、预计攻击伤害和攻击次数
- 遗物稳定 ID、可见计数、原生保存属性以及战斗/回合运行状态
- 药水、金币、卡组、路线、房间、事件、奖励和商店信息
- 角色、难度、Seed、Act、楼层、游戏模式、启用内容和版本指纹

卡牌附魔被放在对应卡牌实例内部，因为附魔与单张卡牌绑定。遗物则保留通用身份字段、原生保存字段、可见计数和运行状态，避免为每个遗物人工设计完全不同的数据结构。

训练视图与审计视图严格分离。训练视图只允许玩家当时可见的信息；抽牌顺序、隐藏 RNG、未来事件和未揭示房间等内容不能进入模型输入。内部状态仅用于检查和回放。

### 3. SL、退出重进与 rollback 语义

《杀戮尖塔 2》在战斗或选择过程中退出重进时，可以回到较早的房间状态。项目将这种行为视为 rollback，而不是一条普通的连续状态转移。

当前处理原则如下：

- 每次重载产生新的尝试编号。
- 被 SL 推翻的旧分支保留在原始记录中，便于研究玩家纠错和搜索行为。
- 旧分支会标记为非 canonical，默认不进入模仿学习数据。
- 最终实际保留下来的分支标记为 canonical，可进入默认训练集。
- rollback 边界会截断 transition，禁止把 SL 前后的状态错误连接成一步环境转移。
- 无法精确匹配的恢复不会被伪装成正常样本，而是进入隔离状态等待审计。

最近审计的三局共有 1,137 个原始决策点，其中 985 个是 canonical 且可训练的决策；152 个旧分支决策因 SL 被正确排除。共检测到 26 次 rollback，全部成功匹配，没有悬空 rollback。

默认设想是不允许最终部署的标准 Agent 使用 rollback。人类 SL 数据可以用于研究纠错、候选动作搜索或训练辅助价值模型，但不能与正常单次决策数据混为一谈。

### 4. Mod 内容隔离

测试过程中曾遇到新角色 Mod 影响选牌池的情况。当前记录会保存已加载内容及实体来源，并区分：

- 原版内容样本
- 进程中启用了 Mod、但当前决策仍属于原版内容的样本
- 实际出现 Mod 卡牌、遗物、敌人或其他实体的样本

默认训练集优先使用可确认的原版内容。涉及 Mod 实体的决策不会被静默丢弃，但必须进入单独分区，避免污染基础游戏策略。

### 5. 特殊机制和边界测试

目前已经针对多种容易漏记的机制进行了实际对局测试，包括：

- 多份战斗奖励
- 四选一卡牌奖励
- 营火多选项或连续选择
- 特殊遗物触发和遗物计数变化
- 卡牌附魔
- 事件商店购买
- 假商人战斗事件
- 污浊药水及其他药水行为
- 丢弃药水
- 选择后取消、重新选择和确认
- 第一回合自动按顺序出牌等特殊控制效果
- 战斗、选牌和其他界面的多次 SL
- 怪物治疗行为

最近发现并修复的一个边界是：玩家在卡牌奖励界面使用非战斗药水时，动作本身合法，但旧版本缺少当前前台奖励界面和来源楼层。0.5.3 已补充这部分上下文。仍建议用一次真实对局专门复测该场景。

### 6. 性能和玩家体验

记录器采用后台写入，避免在每个动作发生时同步写入大量 JSON。经过优化后，开局和游戏过程中的卡顿已经明显减轻。

游戏内有一个小型状态 UI，位置在屏幕右侧中间偏上，约为垂直方向的 3/5。它只显示 Act、楼层和简洁记录状态，不再持续显示回合数，以降低刷新频率和性能影响。

建议使用的记录状态语义包括：

- 正在记录
- 已暂停或尚未进入对局
- 正在恢复/等待匹配
- 记录异常

UI 只承担状态提示，不应进行高频场景扫描或复杂统计。

### 7. 数据审计和批量处理

人工数据不需要逐局手工阅读。当前思路是先进行批量自动审计，再只人工查看未通过或统计异常的少量对局。

自动审计主要检查：

- 文件头、终局和哈希链是否完整
- 决策编号是否连续
- 执行动作是否属于当时合法动作集合
- 动作参数是否完整且能对应到具体实体
- rollback 是否正确闭合
- canonical 与训练资格标记是否一致
- 是否存在 Mod 内容污染
- 是否存在隐藏信息泄漏
- 战斗、路线、商店、事件等奖励阶段是否存在异常缺口
- 记录进程是否发生后台写入错误

目前公开前最后一次检查中，最新三局全部通过自动审计。安装目录中当时共保存 7 个完整 JSONL，合计约 168 MB。这个数字只是 2026-08-08 的阶段快照，后续应重新统计，而不是把它当作最终数据量。

## 四、已经确定的数据原则

1. 原始 JSONL 是不可变证据，不为了修复旧版本而原地改写。
2. 标准化数据必须从原始记录生成，不能绕过原始层直接导出。
3. 不在原始数据中写入人为设计的单一 reward，只保存生命、金币、卡组、遗物、药水、楼层和终局等事实变化。
4. Value target、胜率、未来生命损失和长期收益由后续训练阶段派生。
5. 每个动作必须保存当时完整的合法动作集合。
6. 稳定实体 ID 用于学习，本地化名称仅用于展示和人工检查。
7. 同名卡牌必须依靠实例 ID 区分。
8. SL 旧分支、Mod 内容、无法匹配的恢复和不完整记录必须显式隔离。
9. Seed 可以保存在局级背景和审计信息中，但不应直接作为策略模型的普通观察特征。
10. 任何版本升级都要建立新的兼容性门禁和数据版本分区。

## 五、模型路线设想

### 阶段 A：先做战斗 Agent

战斗相比局外规划更短、更容易验证，动作空间也相对固定，适合作为第一个模型目标。

建议先使用人工对局做行为克隆预训练，让模型学会基本出牌习惯、费用管理、格挡与伤害权衡、目标选择、敌人规律和药水时机。怪物稳定 ID、可见行动意图和预计伤害应显式提供给 Agent。即使怪物规律可以从数据中学习，显式意图仍是正常玩家可见信息，能够显著降低样本需求。

初始目标不是一次覆盖整座塔，而是先验证：

- 固定战斗状态下，模型能否给出合法动作。
- 模型能否明显优于随机和简单启发式。
- 模型能否在常见战斗上接近人工动作分布。
- 模型能否处理未见过的卡组组合、遗物和敌人状态。

随后可加入真实引擎搜索、MCTS 或 Value-guided search。候选动作由策略模型缩小，真实引擎或战斗模型负责展开，价值模型负责评估叶节点。

### 阶段 B：建立战斗 Value Model

战斗 Value 不应只预测当前回合伤害。可考虑同时预测：

- 战斗胜率
- 剩余回合数
- 战斗结束时剩余生命
- 药水消耗
- 关键资源和卡牌状态
- 死亡风险

多任务目标通常比单一标量更容易诊断，也能为搜索提供更稳定的叶节点估值。

### 阶段 C：局外规划模型

抓牌、路线和商店是 World Model/Value Model 的主要应用场景。这里真正关心的是一个选择对未来多个战斗和资源状态的影响。

建议先从离线预测任务开始，而不是一开始生成完整未来：

- 预测若干楼层后的生命、金币、药水、卡组和遗物状态
- 预测下一场精英/Boss 前的资源状态
- 预测 Act 通关率与整局胜率
- 预测某次抓牌或购买在后续是否被频繁使用
- 预测路线风险和资源收益分布

对于每个候选抓牌、路线或商店动作，可以使用模型展开多个可能未来，再用 Value estimation 排序。随机事件和未知路线应输出概率分布或多种可能结果，而不是假装未来完全确定。

### 阶段 D：战斗与局外联合

最终可以形成分层 Agent：

- 上层规划器负责路线、抓牌、商店、事件和资源预算。
- 下层战斗 Agent 负责具体战斗。
- 战斗 Value 为局外规划器提供“这个卡组面对未来敌人的真实战斗能力”。
- 局外 Value 为战斗 Agent 提供跨战斗资源价值，例如是否值得消耗药水或生命。

这比用一个巨大模型同时解决所有阶段更容易训练、审计和替换。

## 六、数据规模建议

当前数据量足以验证记录、清洗和小规模行为克隆流程，但不足以声称具有稳定的强战斗能力。

建议按“战斗决策点和局面覆盖”统计，而不只统计爬塔局数。一个粗略阶段目标是：

- 管线验证：20 至 50 局高质量对局
- 首个战斗行为克隆基线：约 100 至 300 局，尽量覆盖多个难度和不同卡组
- 较稳定的战斗预训练：约 500 至 1,000 局，或达到数万至十余万有效战斗决策点
- 长期规划模型：需要更多完整爬塔结局、抓牌、路线和商店样本，尤其需要成功与失败对局的多样性

可以收集不同难度的铁甲战士对局增加多样性，但不能忽略难度标签。早期应避免角色过多导致每个角色的数据都过于稀疏。优先把铁甲战士做深，再扩展其他角色。

数据质量优先于盲目增加数量。优秀玩家、完整对局、较少无意义 SL、明确版本和丰富局面覆盖，对初始模型更有价值。随机策略产生的数据可以用于学习环境动力学和合法动作，但很难单独学到优秀的抓牌、路线或商店策略。

## 七、当前尚未解决的问题

1. 0.5.3 新加入的“奖励界面使用非战斗药水”上下文还需要真实游戏复测。
2. 仍可能存在非常少见的事件、遗物、药水和多阶段选择没有覆盖。
3. 自动审计能发现结构错误，但“动作语义虽然合法、上下文却不够完整”的问题仍需要统计异常检测和抽样复核。
4. 游戏更新后，动作钩子和原生状态字段可能变化，需要专门的迁移检查。
5. 当前人工数据量仍小，尚未开始正式训练战斗 Agent。
6. World Model 的具体状态编码、随机性建模方式和长期 Value target 尚未冻结。
7. 需要制定玩家数据授权、隐私说明、数据版本和贡献者元数据规则。
8. 公开仓库目前不包含游戏文件和预编译 Mod；换电脑后必须重新合法安装游戏并配置本地依赖。

## 八、换电脑后的建议恢复顺序

下一次项目对话建议按以下顺序接手：

1. 阅读本交接说明，确认仍以 v0.107.1 和铁甲战士为当前基线。
2. 从 GitHub 获取公开源码，并确认提交版本。
3. 在新电脑合法安装《杀戮尖塔 2》，确认游戏版本和程序集哈希。
4. 恢复私人 v0.107.1 备份，但不要放入公开版本控制。
5. 建立 Python 与 .NET 环境，运行全部自动测试。
6. 构建 HumanRecorder，并完成一次启动、地图、战斗、选牌、保存、SL 和终局 smoke test。
7. 复制历史人工记录到独立私有数据位置，先只读批量审计，不修改原始文件。
8. 重新统计总局数、有效决策数、角色、难度、胜负和各阶段覆盖率。
9. 专门复测奖励界面使用非战斗药水的 0.5.3 修复。
10. 在数据管线确认稳定后，再开始战斗 Agent 的最小行为克隆实验。

## 九、下一阶段最小可交付目标

推荐把下一阶段目标定义为“战斗 Agent Baseline V0”，而不是立即训练完整 World Model。

完成标准建议为：

- 批量审计全部现有人工对局，并生成可重复的数据统计报告。
- 冻结第一版战斗 observation、action 和 mask 规范。
- 从 canonical、原版、完整决策中构建训练/验证/测试集，按整局划分避免泄漏。
- 训练一个能够始终输出合法动作的行为克隆模型。
- 与随机策略、简单启发式和人工动作预测准确率进行比较。
- 在真实引擎固定种子战斗上进行离线或受控在线评估。
- 记录失败类型：非法动作、目标错误、费用浪费、致死判断错误、长战斗资源管理错误。
- 基线稳定后，再加入 Value Model 与 MCTS。

## 十、给下一次项目对话的简短提示

这是一个面向《杀戮尖塔 2》v0.107.1 的 AI 项目。当前最成熟的成果是 HumanRecorder 0.5.3 和人工数据审计流程；它已经可以记录战斗与主要局外决策，处理 SL/rollback，隔离 Mod 内容，并保存卡牌附魔、遗物状态、Seed 和版本信息。公开源码已上传 GitHub，游戏本体和玩家数据保持私有。下一步应先在新电脑恢复环境、批量审计历史数据、复测非战斗药水边界，然后冻结战斗数据规范并训练第一个行为克隆战斗 Agent。不要立即把隐藏引擎状态放进训练输入，也不要把 SL 旧分支当成正常 transition。
